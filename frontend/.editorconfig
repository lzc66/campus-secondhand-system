# 统一编码与行尾:此前源码中出现过 UTF-8 被按 GBK 解码的乱码正则,统一 UTF-8 保存
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
indent_style = space
indent_size = 2

[*.md]
trim_trailing_whitespace = false
